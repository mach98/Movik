import React, {FC} from "react";
import {Text, View, FlatList} from 'react-native';
import data from "../../data/mockData";

const GenreBar: FC = () => {
    return (
        <View>
            
        </View>
    )
}

export default GenreBar;

//Create a flatlist, render genre as an array in horizontal position