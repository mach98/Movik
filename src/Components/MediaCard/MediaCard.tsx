import React from 'react';
import {View, TouchableOpacity, Image} from 'react-native';

const MediaCard = () => {
  return (
    <TouchableOpacity>
      <View>
        <Image source={} />
      </View>
    </TouchableOpacity>
  );
};

export default MediaCard;
